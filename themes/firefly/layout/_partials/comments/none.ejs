<%
/**
 * Default / fallback comments partial.
 * This is a placeholder that can be overridden by a specific
 * comment system partial (e.g., giscus.ejs, waline.ejs, etc.).
 *
 * Place actual comment system partials in:
 *   _partials/comments/<comment-type>.ejs
 *
 * The comment type is configured in theme.comments.type in _config.yml.
 */
%>

<div class="comments-container">
  <!-- Comment system not configured or comments disabled -->
  <!-- Set theme.comments.type in your theme _config.yml to enable comments -->
</div>
